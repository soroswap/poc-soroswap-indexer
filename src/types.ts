export interface WorkerResult {
  address: string;
  reserveA: bigint;
  reserveB: bigint;
}
export interface PairEntry {
  address: string;
  tokenA: string;
  tokenB: string;
  reserveA: bigint;
  reserveB: bigint;
}